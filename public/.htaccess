RewriteEngine On
Options -MultiViews

# Let real files/folders pass through
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Internally rewrite /something to index.php?page=something
RewriteRule ^([a-zA-Z0-9_-]+)$ index.php?page=$1 [L]
